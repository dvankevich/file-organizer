export function scanDirectory(directory) {
  console.log(`[Scanner] Сканування директорії: ${directory}`);
  // TODO: реалізувати рекурсивне сканування та збір статистики (кількість, розміри, типи, вік)
}