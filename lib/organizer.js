export function organizeFiles(directory, targetDirectory) {
  console.log(`[Organizer] Сортування файлів з "${directory}" до "${targetDirectory}"`);
  // TODO: копіювання та сортування за категоріями (Documents, Images, Archives, Code, Videos, Other)
}